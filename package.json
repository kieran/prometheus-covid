{
  "name": "prometheus-covid",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx coffee index.coffee",
    "dev": "npx nodemon index.coffee",
    "build": "npx coffee -c -b index.coffee",
    "deploy": "npm run build && gcloud functions deploy prometheus-covid --gen2 --region=us-west1 --runtime=nodejs16 --source=. --entry-point=handler --trigger-http"
  },
  "author": "Kieran Huggins",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "coffeescript": "^2.5.1",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
